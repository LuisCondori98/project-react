const ItemListContainer = ({ greetinging }) => {
    // const { greeting } = props
    return (
        <h1 style={{backgroundColor: "white", padding: "20px"}}>{greetinging}</h1>
    )
}

export default ItemListContainer