specVersion: "v1beta"
serviceId: "project-react"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "project-react-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
